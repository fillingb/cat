<template>
  <div class="min-h-screen flex flex-col bg-[#FFF9F9] text-rose-950 font-sans selection:bg-rose-200 overflow-hidden relative text-center px-4">
    <!-- 氛围感流光背景 -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
      <div class="absolute -top-20 -left-20 w-80 h-80 bg-rose-200/40 rounded-full blur-[100px] animate-pulse"></div>
      <div class="absolute top-1/3 -right-20 w-96 h-96 bg-orange-100/30 rounded-full blur-[120px]"></div>
    </div>

    <!-- 顶部导航 -->
    <header class="relative z-10 flex-shrink-0 pt-10 pb-6 text-center">
      <div class="inline-flex items-center gap-2 px-3 py-1 bg-white/40 backdrop-blur-md rounded-full border border-white/50 mb-4 shadow-sm">
        <span class="flex h-2 w-2 rounded-full bg-rose-400 animate-ping"></span>
        <span class="text-[10px] font-black tracking-widest text-rose-500 uppercase">AI Beauty Intel</span>
      </div>
      <h1 class="text-3xl font-black tracking-tight bg-gradient-to-b from-rose-500 to-rose-700 bg-clip-text text-transparent">猫咪颜究所</h1>
      <p class="text-xs text-rose-400 mt-2 font-medium tracking-wide">集美专供 · 品相避坑 · 颜值分析 ✨</p>
    </header>

    <main class="relative z-10 flex-1 max-w-md mx-auto w-full">
      <!-- 步骤 1：首页交互 (瓷砖选择) -->
      <section v-if="step === 'home'" class="space-y-10 animate-in fade-in slide-in-from-bottom-8 duration-1000">
        <div class="bg-white/40 backdrop-blur-2xl rounded-[2.5rem] p-8 shadow-2xl border border-white/60">
          <label class="block text-[10px] font-black text-rose-300 mb-6 uppercase tracking-[0.2em] px-1 text-center font-black">Step 1: 选择测评品种 Breed</label>
          <div class="grid grid-cols-2 gap-3">
            <button v-for="b in breeds" :key="b" @click="breed = b" :class="breed === b ? 'bg-rose-500 text-white shadow-lg shadow-rose-200 scale-[1.02]' : 'bg-white/60 text-rose-400 hover:bg-white/80'" class="flex items-center justify-center gap-2 py-4 px-2 rounded-2xl font-bold text-xs transition-all duration-300 border border-white/40">
              <span class="text-base">{{ b==='自动识别'?'🔍':b==='布偶猫'?'👸':b==='英国短毛猫'?'🐱':b==='美国短毛猫'?'🐾':'✨' }}</span>
              {{ b }}
            </button>
          </div>
        </div>

        <div class="space-y-4">
          <button @click="openCamera" class="w-full py-6 rounded-3xl bg-rose-500 text-white font-black shadow-lg shadow-rose-200 active:scale-95 transition-all flex items-center justify-center gap-3 relative overflow-hidden group">
            <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
            <span class="text-2xl">📸</span><span class="tracking-widest font-black uppercase">开启颜值评测</span>
          </button>
          <input ref="fileInput" type="file" accept="image/*" capture="environment" class="hidden" @change="onFileSelected" />
          <button @click="$refs.fileInput.click()" class="w-full py-6 rounded-3xl bg-white/60 backdrop-blur-md border border-white text-rose-400 font-bold hover:bg-white/80 active:scale-95 transition-all flex items-center justify-center gap-3 shadow-sm">
            <span class="text-xl">🎞️</span><span class="tracking-widest font-black uppercase">从手机相册选</span>
          </button>
        </div>
        <div class="px-4 py-6 rounded-3xl bg-rose-100/30 border border-rose-200/20 text-center"><p class="text-[10px] text-rose-300 font-medium leading-relaxed uppercase tracking-wider">基于 CFA 专业标准与 AI 深度审美算法</p></div>
      </section>

      <!-- 步骤 2：加载中 -->
      <section v-if="step === 'loading'" class="flex flex-col items-center justify-center py-32 animate-in zoom-in-95 duration-700">
        <div class="relative w-32 h-32">
          <div class="absolute inset-0 rounded-full border-[10px] border-white shadow-lg"></div>
          <div class="absolute inset-0 rounded-full border-[10px] border-t-rose-400 animate-spin"></div>
          <div class="absolute inset-0 flex items-center justify-center text-3xl animate-pulse">💖</div>
        </div>
        <div class="mt-12 text-center">
          <h3 class="text-xl font-black text-rose-500 mb-2">{{ loadingText }}</h3>
          <p class="text-[10px] text-rose-300 font-bold uppercase tracking-widest">Processing with AI Intel</p>
        </div>
      </section>
      <section v-if="step === 'result' && result" id="result-page-placeholder"></section>
    </main>
    <footer class="relative z-10 py-10 text-center opacity-30 text-[10px] font-black text-rose-500 uppercase tracking-widest">AI Beauty Lab · XHS Edition</footer>
  </div>
</template>
